node_modules/
dist/
.env
.env.local
.env*.local
export.geojson
*.local
.DS_Store

# Unused / archived datasets (large files, not needed by the website)
datasets/_unused/

# OSM pipeline cache & intermediate files (regenerated by fetch_osm_datacenters.py)
datasets/osm/raw/
datasets/osm/osm_datacenters.geojson
